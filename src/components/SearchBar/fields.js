
const fields = {
    query: {
        className: "SearchForm-input",
        type: "text",
        autoComplete: "off",
        autoFocus: true,
        placeholder: "Search images and photos"
    }
   } 

export default fields